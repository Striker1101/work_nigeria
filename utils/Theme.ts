import { ThemeOptions } from "@mui/material/styles";

const theme: ThemeOptions = {
  palette: {
    primary: {
      main: "black",
      contrastText: "white",
      dark: "black",
      light:"white"
    },
  },
};

export default theme;
